#include <vector>
#include <trip_modules.h>

vector<Schedule> ScheduleModule::total_trip_schedule(string first_place,vector<Place> selected_places, vector<Transport> selected_transports, int total_time_limit) {
    vector<Schedule> schadules_way;
        
    return schadules_way;
}